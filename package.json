{
  "name": "nudget-desktop",
  "main": "electron.js",
  "scripts": {
    "dev": "electron .",
    "start:dev": "electron --force-node-api-uncaught-exceptions-policy=true .",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^26.0.0"
  },
  "dependencies": {
    "wait-on": "^8.0.3"
  }
}
